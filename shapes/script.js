const { innerWidth: width, innerHeight: height } = window;

let shape;
function setup() {
    createCanvas(width, height)
    background(0)
    shape = new Shape(width / 2, height / 2)
}

function draw() {
    // start making shapes!

}